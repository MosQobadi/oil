generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Oil {
  id          Int      @id @default(autoincrement())
  name        String
  brand       String
  viscosity   String
  price       Float
  createdAt   DateTime @default(now())
}